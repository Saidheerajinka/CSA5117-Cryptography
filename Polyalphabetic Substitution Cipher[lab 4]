text = input("Enter text: ").upper()
key = input("Enter key: ").upper()

cipher = ""

for i in range(len(text)):
    t = ord(text[i]) - ord('A')
    k = ord(key[i % len(key)]) - ord('A')
    cipher += chr((t + k) % 26 + ord('A'))

print("Encrypted text:", cipher)
